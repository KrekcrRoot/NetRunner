import {AxiosInstance} from "axios";

export default (instance: AxiosInstance) => {
    return {
    //     Действительно, а нахуй нам нужен артикл? пошли нахуй на него запросы их не существуют
    }
}